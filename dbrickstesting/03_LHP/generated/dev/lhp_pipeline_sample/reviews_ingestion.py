# Generated by LakehousePlumber
# Pipeline: lhp_pipeline_sample
# FlowGroup: reviews_ingestion

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "lhp_pipeline_sample"
FLOWGROUP_ID = "reviews_ingestion"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_lhp_reviews_cloudfiles():
    """Load lhp_reviews Parquet files from landing volume"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "parquet")
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
        .option("cloudFiles.rescuedDataColumn", "_rescued_data")
        .load("/Volumes/pilotws/pilotschema/pilotvolume/reviews/")
    )

    # Add operational metadata columns
    df = df.withColumn("_source_file_path", F.col("_metadata.file_path"))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="pilotws.pilotschema.lhp_reviews", comment="Streaming table: lhp_reviews"
)


# Define append flow(s)
@dp.append_flow(
    target="pilotws.pilotschema.lhp_reviews",
    name="f_lhp_reviews_cloudfiles",
    comment="Write lhp_reviews to bronze layer",
)
def f_lhp_reviews_cloudfiles():
    """Write lhp_reviews to bronze layer"""
    # Streaming flow
    df = spark.readStream.table("v_lhp_reviews_cloudfiles")

    return df
