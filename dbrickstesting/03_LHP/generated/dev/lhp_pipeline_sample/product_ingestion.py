# Generated by LakehousePlumber
# Pipeline: lhp_pipeline_sample
# FlowGroup: product_ingestion

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "lhp_pipeline_sample"
FLOWGROUP_ID = "product_ingestion"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_lhp_products_cloudfiles():
    """Load lhp_products Parquet files from landing volume"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "parquet")
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
        .option("cloudFiles.rescuedDataColumn", "_rescued_data")
        .load("/Volumes/pilotws/pilotschema/pilotvolume/products/")
    )

    # Add operational metadata columns
    df = df.withColumn("_source_file_path", F.col("_metadata.file_path"))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="pilotws.pilotschema.lhp_products", comment="Streaming table: lhp_products"
)


# Define append flow(s)
@dp.append_flow(
    target="pilotws.pilotschema.lhp_products",
    name="f_lhp_products_cloudfiles",
    comment="Write lhp_products to bronze layer",
)
def f_lhp_products_cloudfiles():
    """Write lhp_products to bronze layer"""
    # Streaming flow
    df = spark.readStream.table("v_lhp_products_cloudfiles")

    return df
