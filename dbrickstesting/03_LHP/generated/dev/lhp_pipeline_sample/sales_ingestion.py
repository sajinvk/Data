# Generated by LakehousePlumber
# Pipeline: lhp_pipeline_sample
# FlowGroup: sales_ingestion

from pyspark import pipelines as dp
from pyspark.sql import functions as F

# Pipeline Configuration
PIPELINE_ID = "lhp_pipeline_sample"
FLOWGROUP_ID = "sales_ingestion"


# ============================================================================
# SOURCE VIEWS
# ============================================================================


@dp.temporary_view()
def v_lhp_sales_cloudfiles():
    """Load lhp_sales Parquet files from landing volume"""
    df = (
        spark.readStream.format("cloudFiles")
        .option("cloudFiles.format", "parquet")
        .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
        .option("cloudFiles.rescuedDataColumn", "_rescued_data")
        .load("/Volumes/pilotws/pilotschema/pilotvolume/sales/")
    )

    # Add operational metadata columns
    df = df.withColumn("_source_file_path", F.col("_metadata.file_path"))

    return df


# ============================================================================
# TARGET TABLES
# ============================================================================

# Create the streaming table
dp.create_streaming_table(
    name="pilotws.pilotschema.lhp_sales", comment="Streaming table: lhp_sales"
)


# Define append flow(s)
@dp.append_flow(
    target="pilotws.pilotschema.lhp_sales",
    name="f_lhp_sales_cloudfiles",
    comment="Write lhp_sales to bronze layer",
)
def f_lhp_sales_cloudfiles():
    """Write lhp_sales to bronze layer"""
    # Streaming flow
    df = spark.readStream.table("v_lhp_sales_cloudfiles")

    return df
